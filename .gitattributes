# Normalise line endings in the repository to LF; Windows checkouts still work.
* text=auto eol=lf

# Windows launchers must keep CRLF so cmd.exe parses them correctly.
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binary assets
*.png binary
*.wav binary
*.webm binary
